<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="دنقلا كير"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!--
      إصلاح جذري لمشكلة عدم عمل: الاتصال الهاتفي، خرائط Google، واتساب،
      والبريد الإلكتروني.

      اعتباراً من Android 11 (API 30)، أي تطبيق يريد استخدام canLaunchUrl()
      أو launchUrl() للتحقق من تطبيقات أخرى أو فتحها، يجب أن يُصرّح صراحة
      عن أنواع الروابط (intents) التي ينوي فتحها ضمن وسم <queries> هنا.
      بدون هذا التصريح، canLaunchUrl() ترجع false دائماً بصمت — حتى لو كان
      التطبيق المستهدف (واتساب، خرائط جوجل) مثبتاً وشغّالاً فعلياً على
      الجهاز. هذا هو السبب الجذري الذي جعل أزرار الاتصال، الخرائط،
      واتساب، والبريد الإلكتروني تفشل صامتة في التطبيق.
    -->
    <queries>
        <!-- الاتصال الهاتفي: tel: -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>

        <!-- روابط الويب العامة: يشمل واتساب (wa.me) وروابط خرائط جوجل
             الاحتياطية (https://www.google.com/maps/...) -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>

        <!-- خرائط جوجل عبر رابط geo: (الطريقة القياسية لأندرويد) -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>

        <!-- البريد الإلكتروني: mailto: -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>

        <!-- Required to query activities that can process text, see:
             https://developer.android.com/training/package-visibility and
             https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

             In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
